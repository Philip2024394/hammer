import { WorldContent } from "./Content";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Live presence — Hammerex Admin"
};

export default function AdminWorldPage() {
  return <WorldContent />;
}
